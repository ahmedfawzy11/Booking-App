name: booking_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-271.7.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  font_awesome_flutter: ^10.2.1
  smooth_page_indicator: ^1.0.0+2
  cupertino_icons: ^1.0.5
  flutter_bloc: ^8.1.1
  sqflite: ^2.1.0
  path: ^1.8.2
  dio: ^5.0.3
  get_it: ^7.2.0
  dartz: ^0.10.1
  provider: ^6.0.3
  google_fonts: ^3.0.1
  shared_preferences: ^2.0.15
  google_maps_flutter: ^2.2.0
  flutter_rating_bar: ^4.0.1
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/json/
    - lib/Data/Language/lang/languageText.json